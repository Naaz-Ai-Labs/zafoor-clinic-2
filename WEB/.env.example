# Base URL of the CRM's public booking API, consumed by src/lib/crmApi.js.
# Include the /api/public path segment. No trailing slash.
# Defaults to the CRM dev server below when unset.
VITE_CRM_API_URL=http://localhost:3000/api/public
